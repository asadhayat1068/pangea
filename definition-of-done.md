# Definition of Done (Sprint 1)
A ticket is Done when:
- Code is reviewed and merged via PR.
- `go vet`, `go test`, and `go build` pass in CI (the build-test check is green).
- The change is deployed to staging (from Day 8 onward).
- It is demoable against the sprint's Demo Scenario.